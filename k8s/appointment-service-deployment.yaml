apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: appointment-service
  template:
    metadata:
      labels:
        app: appointment-service
    spec:
      containers:
      - name: appointment-service
        image: yourdockerhub/appointment-service
        ports:
        - containerPort: 3002
        env:
          - name: JWT_SECRET
            valueFrom:
              secretKeyRef:
                name: mediqueue-secrets
                key: JWT_SECRET
          - name: MONGO_URI
            valueFrom:
              secretKeyRef:
                name: mediqueue-secrets
                key: MONGO_URI
          - name: RABBITMQ_URL
            valueFrom:
              secretKeyRef:
                name: mediqueue-secrets
                key: RABBITMQ_URL
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 10